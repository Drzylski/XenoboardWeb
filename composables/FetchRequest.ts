import { useFetch } from "nuxt/app"

export const useFetchRequest = (requestMethod:string, requestUrl:string, requestData:object | string) => {

  const fetchRequest = useFetch(requestUrl, )
  
  return null;
}
